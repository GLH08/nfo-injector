# NFO MediaInfo 注入管理器 — 环境变量配置
#
# 复制此文件为 .env，修改路径后在 nfo-injector/ 目录下运行：
#   docker compose up -d
#
# docker-compose 会将 /apps 挂载到容器内的 /apps，
# 然后在 Web UI 配置媒体库时使用容器内绝对路径（/apps/...）

# 仅用于当 config.json 不存在时，自动生成第一个媒体库
STRM_ROOT=/apps/moviepilot/115strm/Emby
MEDIA_ROOT=/apps/clouddrive2/CloudDrive/115open/Media
